# Core
datasets>=3.0.0
huggingface_hub>=0.25.0
pillow>=10.0.0
tqdm>=4.66.0
pyyaml>=6.0
pydantic>=2.0

# Metrics / scoring
numpy>=1.26
pandas>=2.0

# LLM judge + baseline API clients (install only what you need)
openai>=1.40.0
anthropic>=0.34.0
google-generativeai>=0.7.0
httpx>=0.27.0

# Local model runners (optional; install separately)
# sglang
# vllm

# Dev
pytest>=8.0
ruff>=0.6.0
